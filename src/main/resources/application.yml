spring:
  application:
    name: MyUserManager

  datasource:
    url: jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:myUser}
    username: kprahul
    password: kprahul143
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 1

  sql:
    init:
      mode: always

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

jwt:
  secret: OneDayOrDayOne123OneDayOrDayOne123OneDayOrDayOne123OneDayOrDayOne123ckhgvhvltfytfhlgvnvljvlh[hoewifpuwehfipuhewfiuhipufiwe
  expiration: 86400000


logging:
  level:
    root: INFO
  pattern:
    console: ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}

server:
  port: 8090